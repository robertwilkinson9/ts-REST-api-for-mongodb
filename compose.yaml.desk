services:
  desk_backend:
    container_name: desk_backend
    build: .
    ports:
      - "6179:6179"
    environment:
      SSL_CERT: "/certs/localhost.crt"
      SSL_KEY: "/certs/localhost.key"
      MONGO_IP: "mongo"
