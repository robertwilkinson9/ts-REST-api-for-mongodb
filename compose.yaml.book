services:
  book_backend:
    container_name: book_backend
    build: .
    ports:
      - "6180:6180"
    environment:
      SSL_CERT: "/certs/localhost.crt"
      SSL_KEY: "/certs/localhost.key"
      DB_NAME: "book"
      MONGO_IP: "mongo"
